<view class="box" wx:if='{{article.id}}'>
	<view class="title">{{article.name}}</view>
	<view>
		<image src='{{article.src}}' mode="aspectFit"></image>
	</view>
	
	<view class="content">
		{{article.content}}
	</view>
</view>
